patchedDependencies:
  react-typed-i18n@2.6.1: patches/react-typed-i18n.patch

allowBuilds:
  esbuild: true

# Keep the pre-pnpm-12 installation behavior; dependency freshness is managed
# through the committed lockfile and dependency update review.
minimumReleaseAge: 0
